# Resume Bullets

Use these only if they match the role and the surrounding resume context. They are based strictly on the current repository contents.

- Built a Python time-series forecasting pipeline for daily US equity index data using packaged `RNN`, `LSTM`, and `GRU` models, chronological train/validation/test splits, and leakage-safe scaling.
- Refactored a coursework-style project into a reproducible `src/`-layout package with a CLI, editable install, automated tests, and structured experiment artifacts for histories, predictions, plots, and run metadata.
- Implemented robust CSV normalization for mixed market-data schemas, including support for `Close/Last` vs `Price`, optional volume columns, and percent-change parsing across three bundled index datasets.
- Added benchmark-aware reporting that compares neural-network results against a naive persistence baseline and saves dataset profiles plus per-run champion summaries for transparent evaluation.
- Improved project usability with configurable symbol/model subsets, deterministic seeds, smoke-run commands, and documentation backed by verified sample outputs generated from the repository’s existing data.
